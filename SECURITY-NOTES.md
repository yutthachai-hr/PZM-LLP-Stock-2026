# หมายเหตุด้านความปลอดภัยของ dependencies

`npm audit` ในโปรเจกต์นี้**จะไม่ขึ้นศูนย์** และนั่นเป็นเรื่องที่ตั้งใจ ไฟล์นี้อธิบายว่าแต่ละรายการที่เหลือคืออะไร ทำไมถึงยังอยู่ และอะไรจะเปลี่ยนการตัดสินใจนั้น

ทบทวนล่าสุด: **9 กันยายน 2026** — ตอนนั้นเหลือ moderate 17, high 9, critical 0

> ⚠️ **ห้ามรัน `npm audit fix --force`** คำสั่งนั้นจะ *ลดเวอร์ชัน* `firebase-tools` จาก 15.x ไปเป็น 10.1.1 (ข้าม major ย้อนหลังหลายปี) ซึ่งทำให้ emulator และการ deploy rules พังทั้งชุด

---

## xlsx (SheetJS) 0.18.5 — high 2 รายการ ยังคงไว้

- [Prototype pollution (GHSA-4r6h-8v6p-xvw6)](https://github.com/advisories/GHSA-4r6h-8v6p-xvw6)
- [ReDoS (GHSA-5pgg-2g8v-p4x9)](https://github.com/advisories/GHSA-5pgg-2g8v-p4x9)

**ทำไมยังไม่อัปเดต:** SheetJS เลิกเผยแพร่บน npm หลังเวอร์ชัน 0.18.5 เวอร์ชันที่แก้แล้ว (0.19.3 / 0.20.2 ขึ้นไป) มีเฉพาะบน `cdn.sheetjs.com` เท่านั้น การติดตั้งจาก URL นั้นทำให้ `npm ci` บน Netlify ต้องต่อ CDN ภายนอกได้ทุกครั้งที่ build — ถ้า CDN ล่มหรือถูกบล็อก เว็บจะ build ไม่ผ่าน เจ้าของระบบเลือกไม่แลกความเสี่ยงนั้น (9 ก.ย. 2026)

**ทำไมความเสี่ยงจริงต่ำ:** ช่องโหว่ทั้งสองอยู่ที่การ **อ่าน** ไฟล์ (`XLSX.read`) จากแหล่งที่ไม่น่าเชื่อถือ

| ที่ใช้ | ทำอะไร | รับ input จากไหน |
|---|---|---|
| `src/lib/export.ts` (เว็บ) | **เขียน** อย่างเดียว (`json_to_sheet`, `writeFile`) | ข้อมูลในระบบตัวเอง — ไม่เคยอ่านไฟล์จากผู้ใช้ |
| `scripts/import-items.mjs` (dev) | อ่าน workbook | ไฟล์รหัสสินค้าของบริษัทเอง รันบนเครื่อง dev ไม่ใช่ production |

เว็บที่ผู้ใช้เปิด **ไม่มีเส้นทางให้ไฟล์จากภายนอกเข้าไปถึง parser ของ xlsx เลย**

**อะไรจะเปลี่ยนการตัดสินใจ:** ถ้าวันไหนแอปเพิ่มฟีเจอร์ "อัปโหลดไฟล์ Excel เข้าระบบ" ข้อนี้จะกลายเป็นช่องโหว่จริงทันที ต้องเปลี่ยน library หรือย้ายไป CDN ก่อนทำฟีเจอร์นั้น

---

## firebase-tools 15.x — moderate ยังคงไว้

เป็น CLI สำหรับ dev เท่านั้น (`npm run test:rules`, `firebase deploy`) ไม่ได้ถูกรวมเข้าไปในเว็บที่ deploy ช่องโหว่ที่ audit รายงานมาจาก dependency ที่ CLI พาไปด้วย

npm เสนอ "fix" เป็นการถอยไป 10.1.1 ซึ่งเก่ากว่าหลายปีและใช้กับ Firestore rules v2 + emulator ปัจจุบันไม่ได้ — เสียมากกว่าได้

---

## ที่อัปเดตไปแล้ว (9 ก.ย. 2026)

| Package | จาก | เป็น | เหตุผล |
|---|---|---|---|
| `react-router-dom` | 7.18.1 | 7.18.3 | [advisory](https://github.com/advisories/GHSA-qwww-vcr4-c8h2) เกี่ยวกับ RSC API ที่แอปนี้ไม่ได้ใช้ แต่เป็น patch ที่อัปได้ปลอดภัย |
| `vitest` | 4.x | 5.x | dev เท่านั้น — ยืนยันแล้วว่าทั้ง unit tests และ rules tests ผ่านครบหลังอัป |

---

## วิธีทบทวนรอบหน้า

```bash
npm audit
```

อ่านทีละรายการแล้วถามสองคำถาม: **โค้ดที่มีช่องโหว่ถูกเรียกจากเว็บที่ผู้ใช้เปิดไหม** และ **input มาจากคนนอกหรือเปล่า** ตัวเลขรวมจาก `npm audit` นับ package ที่ได้รับผลกระทบ ไม่ใช่จำนวนช่องทางที่โจมตีเว็บนี้ได้จริง
