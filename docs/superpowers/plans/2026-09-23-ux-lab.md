# UX Lab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agents given only a goal work the demo build like real staff, their confusion becomes evidence, and that evidence walks a state machine to a verified fix — without any step able to touch production.

**Architecture:** Everything lives as files in the repo under `docs/ux-lab/`. Personas and scenarios are Markdown. Each agent run writes a report folder. Findings are deduped into issue files indexed by `ledger.json`. A small tested Node module checks that the index and the files agree, that state transitions are legal, and that no HIGH/CRITICAL issue reaches `RELEASED` without the owner's recorded approval. The app itself is not modified except by fixes the loop produces.

**Tech Stack:** Markdown + JSON for the lab · TypeScript (`scripts/ux-lab/*.ts`, type-stripped by Node 24, same pattern as `scripts/i18n-check.mjs`) · vitest for the module's tests · the existing browser pane (`mcp__Claude_Browser__*`) to drive `npm run demo`.

## Global Constraints

- **Every step runs against `npm run demo` (http://localhost:5175) only.** No step in this plan may read or write Firebase project `pzm-stock-x5`. The demo build has no Firebase config at all (`src/backend/index.ts` falls back to `createLocalBackend()`), which is what makes this safe.
- **Scenarios state a goal and never a step.** Copied verbatim from the owner's spec: *"อย่าบังคับ User ทำตาม step UI ให้บอก Goal ของงาน เพื่อให้เห็นว่า UX เข้าใจง่ายจริงหรือไม่"*
- **A finding that cannot be reproduced is never fixed by guessing.** It goes to `NEEDS_MORE_DATA`.
- **No agent pushes `main` and no agent deploys.** Only the owner releases.
- **Risk tiers** (from the owner's spec): `LOW` layout/spacing/copy/responsive · `MEDIUM` search/filter/validation/form interaction · `HIGH` supplier mapping, approved quantity, approval state, purchase order generation · `CRITICAL` stock quantity, financial data, destructive migration, authentication/security.
- **No paid SaaS.** No analytics platform, session replay, bug tracker or automation service is added.
- **Repo files are CRLF.** Scripted edits must preserve them.
- **The QA gate is the existing pre-commit gate, unchanged:** `npx tsc -p tsconfig.app.json --noEmit && npm run lint && npm run i18n:check && npm test && npm run test:rules && npx tsc -p worker/tsconfig.json --noEmit && npm run build`.
- Thai is the language of personas, scenarios and run reports, because that is the language the staff think in. Code, identifiers and commit messages stay English.

---

## File Structure

| File | Responsibility |
|---|---|
| `docs/ux-lab/README.md` | The lab's constitution: the loop, the forbidden lines, the risk table, the state machine |
| `docs/ux-lab/personas/staff-branch.md` | Who the branch staffer is and what they do **not** know |
| `docs/ux-lab/personas/manager.md` | Who the manager is and what they care about |
| `docs/ux-lab/scenarios/SC-0NN-<slug>.md` | One goal each, with front-matter naming persona, viewport and routes |
| `docs/ux-lab/runs/<date>-<scenario>-<persona>-<viewport>/report.md` | What happened in one run, plus the seven metrics |
| `docs/ux-lab/runs/.../shots/` | Screenshots taken at the moments the agent got stuck |
| `docs/ux-lab/issues/UX-NNNN-<slug>.md` | One deduped issue, human-readable |
| `docs/ux-lab/ledger.json` | Machine-readable index of every issue |
| `scripts/ux-lab/ledger.ts` | Pure: issue types, the state machine, dedupe keys, ledger validation |
| `scripts/ux-lab/coverage.ts` | Pure: which app routes no scenario reaches |
| `scripts/ux-lab/report.ts` | Pure: ledger → status page HTML |
| `scripts/ux-lab/cli.mjs` | Thin: reads the files, calls the pure modules, prints, sets exit code |
| `scripts/tsconfig.json` | Typechecks `scripts/` the way `worker/tsconfig.json` typechecks the Worker |
| `tests/ux-lab-ledger.test.ts` | Tests for `ledger.ts` |
| `tests/ux-lab-coverage.test.ts` | Tests for `coverage.ts` |
| `tests/ux-lab-report.test.ts` | Tests for `report.ts` |

---

## Task 1: The lab's constitution

**Files:**
- Create: `docs/ux-lab/README.md`

**Interfaces:**
- Consumes: nothing.
- Produces: the vocabulary every later task uses — state names, category names, severity names, risk tiers, run-folder naming.

- [ ] **Step 1: Write the README**

Create `docs/ux-lab/README.md` with exactly these sections, in this order:

1. `# UX Lab` and one paragraph: the owner is the middleman between staff who struggle and the developer who could fix it; this removes that middleman.
2. `## วงจร` — the loop as a list: `รัน scenario → รายงาน → Issue → reproduce → patch เล็กที่สุด → regression test → ด่าน QA → branch demo → retest → VERIFIED → เจ้าของสั่ง merge → RELEASED → CLOSED`.
3. `## เส้นที่ห้ามข้าม` — the three rules, each as its own bolded line:
   - **ทุกอย่างวิ่งบน `npm run demo` เท่านั้น** — ไม่มีขั้นตอนไหนแตะ `pzm-stock-x5` ได้
   - **reproduce ไม่ได้ ห้ามแก้โค้ดแบบเดา** — ลง `NEEDS_MORE_DATA` แล้วเก็บข้อมูลเพิ่ม
   - **ห้าม push `main` ห้าม deploy** — เจ้าของเป็นคนสั่งเท่านั้น
4. `## Risk` — the four-row table from Global Constraints, with the "agent ทำได้ถึงไหน" column: `LOW` แก้+ทดสอบ+ลง branch `demo` ได้เอง · `MEDIUM` เท่ากัน + ต้อง retest scenario เดิมให้ผ่าน · `HIGH` สร้าง patch ได้ แต่ขึ้น `main` ต้องเจ้าของอนุมัติ · `CRITICAL` ห้ามขึ้นอัตโนมัติเด็ดขาด
5. `## Severity ไม่ใช่ Risk` — one short paragraph: `severity` คือปัญหาขวางงานแค่ไหน (`blocker` / `major` / `minor` / `cosmetic`), `risk` คือการแก้อันตรายแค่ไหน; ปุ่มเบี้ยวในหน้าอนุมัติมี severity ต่ำแต่ risk สูง.
6. `## Category` — the six categories with one line each, and the rule in bold: **`TRAINING_ISSUE` = ระบบถูกแต่คนไม่เข้าใจ → ห้ามแก้โค้ด เสนอได้แค่เปลี่ยนถ้อยคำ**.
7. `## State` — the transition list, one arrow per line, copied from Task 6's `NEXT_STATES`.
8. `## ชื่อโฟลเดอร์ของ run` — `YYYY-MM-DD-SC-0NN-<persona>-<viewport>`, viewport เป็น `desktop` หรือ `mobile` เท่านั้น.
9. `## คำสั่ง` — a fenced `bash` block containing `npm run ux-lab` and one line saying it checks the ledger, the issue files and scenario coverage.

- [ ] **Step 2: Verify the file is CRLF and has no placeholder text**

Run:
```bash
cd pizza-stock && file docs/ux-lab/README.md && grep -n "TBD\|TODO\|XXX" docs/ux-lab/README.md || echo "clean"
```
Expected: `with CRLF line terminators`, then `clean`.

- [ ] **Step 3: Commit**

```bash
git add docs/ux-lab/README.md
git commit -m "UX lab: the constitution the loop runs under"
```

---

## Task 2: Personas

**Files:**
- Create: `docs/ux-lab/personas/staff-branch.md`
- Create: `docs/ux-lab/personas/manager.md`

**Interfaces:**
- Consumes: the vocabulary from Task 1.
- Produces: persona ids `staff-branch` and `manager`, referenced by every scenario's front-matter and every run folder name.

- [ ] **Step 1: Write `staff-branch.md`**

Front-matter then prose:

```markdown
---
id: staff-branch
role: staff
device: โทรศัพท์เป็นหลัก คอมที่ร้านบ้าง
---

# พนักงานสาขา

ทำงานหน้าร้าน รับของตอนเช้า เบิกของเข้าครัว นับสต๊อกตอนปิดร้าน
และบอกให้สั่งของเพิ่มเมื่อเห็นว่าใกล้หมด

## สิ่งที่รู้

- รู้ว่าของหน้าตาเป็นยังไง และเรียกด้วยชื่อเล่น — "ผักร็อกเก็ต" "โอ๊คแดง" "โค้กกระป๋อง"
- รู้ว่าของนี้ใช้วันละประมาณเท่าไร
- รู้ว่าวันไหนของมาส่ง

## สิ่งที่ไม่รู้ — ข้อนี้สำคัญที่สุด

- **ไม่เคยอ่านคู่มือ** และจะไม่อ่าน
- **ไม่รู้รหัสสินค้า** — `VGT-01-03-002` ไม่มีความหมายอะไรเลย
- **ไม่รู้ว่าของมาจากผู้ขายเจ้าไหน** — เห็น `ROCKET SALAD (ACK)` กับ `ROCKET SALAD (KIN-D)` แล้วไม่รู้ว่าต่างกันยังไง ควรเลือกอันไหน
- **ไม่รู้ศัพท์ในระบบ** — ไม่แน่ใจว่า "ปรับสต๊อก" กับ "นับสต๊อก" ต่างกันยังไง หรือ "เบิก" กับ "โอน" อันไหนใช้ตอนไหน
- **ไม่รู้ว่าเมนูไหนทำอะไร** จนกว่าจะกดเข้าไปดู

## วิธีคิดเวลาติด

ลองกดดูก่อน ถ้ากดผิดจะถอยกลับแล้วลองอันอื่น ถ้าลองสามสี่ครั้งแล้วยังไม่ได้
จะเลิกแล้วไปบอกหัวหน้าด้วยปากแทน — **ตอนนั้นคือ `abandoned`**
```

- [ ] **Step 2: Write `manager.md`**

```markdown
---
id: manager
role: manager
device: คอมเป็นหลัก โทรศัพท์ตอนไม่อยู่ร้าน
---

# หัวหน้า

ดูรายการที่ลูกน้องขอ ตัดจำนวนที่ขอมาเกิน เพิ่มของที่ลูกน้องลืม
อนุมัติ แล้วออกใบสั่งซื้อส่งผู้ขาย

## สิ่งที่สนใจ

- ของนี้สั่งเยอะไปไหมเมื่อเทียบกับที่ใช้จริง
- เมื่อวานสั่งเจ้านี้ไปแล้วหรือยัง จะได้ไม่สั่งซ้ำ
- ของหมดจริงหรือแค่ลืมนับ
- ใบสั่งซื้อที่ส่งไปแล้วแก้ทีหลังได้ไหม ถ้าแก้ ผู้ขายจะรู้ไหม

## สิ่งที่ไม่รู้

- **ไม่รู้ว่าระบบคิดยังไง** — ไม่รู้ว่าทำไมบางรายการสั่งไม่ได้ ไม่รู้ว่า "ร่าง" ต่างจาก "สั่งแล้ว" ยังไง
- **ไม่รู้ว่าแก้แล้วย้อนได้ไหม** จึงลังเลทุกครั้งที่จะกดปุ่มที่ฟังดูถาวร
- **ไม่เคยอ่านคู่มือ**

## วิธีคิดเวลาติด

อ่านหน้าจอให้ละเอียดกว่าพนักงาน แต่ถ้าระบบไม่บอกว่าจะเกิดอะไรขึ้นหลังกด
จะไม่กด แล้วไปหาทางอื่นแทน — **การไม่กล้ากดคือปัญหา ไม่ใช่ความระมัดระวัง**
```

- [ ] **Step 3: Commit**

```bash
git add docs/ux-lab/personas
git commit -m "UX lab: two personas defined by what they do not know"
```

---

## Task 3: Scenarios covering every page

**Files:**
- Create: `docs/ux-lab/scenarios/SC-001-purchase-request.md` … `SC-016-entry-units.md` (16 files)

**Interfaces:**
- Consumes: persona ids from Task 2.
- Produces: scenario ids `SC-001`…`SC-016` and the `routes:` front-matter list that Task 6's `coverage.ts` reads.

- [ ] **Step 1: Write the scenario template into the first file**

Every scenario file has this exact shape. `routes` must list every route the goal is expected to touch, using the paths as they appear in `src/App.tsx`.

`docs/ux-lab/scenarios/SC-001-purchase-request.md`:

```markdown
---
id: SC-001
persona: staff-branch
viewport: desktop
routes: ['/', '/requests']
---

# ขอให้สั่งผักเพิ่ม

## เป้าหมาย

พรุ่งนี้ผักจะหมด อยากให้สั่งเพิ่ม — ผักร็อกเก็ต 3 กิโล โอ๊คแดง 5 กิโล
และโค้กกระป๋อง 5 แพ็ค ส่งให้หัวหน้าดูให้เรียบร้อย

## ถือว่าเสร็จเมื่อ

มีใบขอสั่งซื้อที่หัวหน้าเห็นได้ มีของครบสามรายการ จำนวนตรงตามที่ตั้งใจ

## ห้าม

ห้ามบอกทางในไฟล์นี้ ถ้า agent หาไม่เจอ นั่นคือผลการทดสอบ ไม่ใช่ความผิดของ agent
```

- [ ] **Step 2: Write the remaining 15 scenarios in the same shape**

Use these goals verbatim. Every one states an outcome, never a screen.

| id | persona | viewport | routes | เป้าหมาย |
|---|---|---|---|---|
| SC-002 | manager | desktop | `['/requests', '/requests/:id', '/orders']` | ลูกน้องขอผักมา ร็อกเก็ตขอมา 3 กิโลแต่ให้แค่ 2 พอ และลืมขอโค้กซีโร่ 5 แพ็ค เพิ่มให้ด้วย แล้วอนุมัติและออกใบสั่งซื้อส่งผู้ขาย สุดท้ายเอารายการออกเป็น Excel |
| SC-003 | staff-branch | desktop | `['/receive']` | ของมาส่งเมื่อเช้า บิลเลขที่ INV-2026-0912 มีชีสมอสซาเรลล่า 10 กิโล กับแป้ง 4 ถุง รับเข้าคลังหลัก |
| SC-004 | staff-branch | desktop | `['/issue']` | เบิกชีสมอสซาเรลล่า 2 กิโลเข้าครัวสาขาสารสิน แล้วส่งแป้ง 1 ถุงไปให้สาขาอ่อนนุชที่ของขาด |
| SC-005 | staff-branch | desktop | `['/adjust']` | ปิดร้านแล้วนับของจริง ชีสเหลือ 7 กิโลแต่ในระบบขึ้น 8 และเจอมะเขือเทศเน่าทิ้งไป 2 กิโล ทำให้ตัวเลขตรงกับของจริง |
| SC-006 | staff-branch | desktop | `['/', '/products']` | อยากรู้ว่าตอนนี้ของอะไรใกล้หมดบ้าง แล้วตั้งจุดสั่งซื้อของชีสไว้ที่ 5 กิโล จะได้เตือนก่อน |
| SC-007 | manager | desktop | `['/products', '/products/:id/card', '/movements']` | ชีสหายไป 3 กิโลโดยไม่รู้สาเหตุ ตามหาว่าหายตอนไหน ใครเป็นคนทำรายการ |
| SC-008 | manager | desktop | `['/reports']` | อยากรู้ว่าเดือนนี้ใช้ของอะไรไปเยอะที่สุด และเทียบกับเดือนก่อนแล้วเพิ่มขึ้นไหม |
| SC-009 | manager | desktop | `['/calendar']` | ตั้งให้มีการนับสต๊อกใหญ่วันสิ้นเดือน มอบหมายให้พนักงานสาขาทุกคน |
| SC-010 | manager | desktop | `['/suppliers']` | มีผู้ขายผักเจ้าใหม่ชื่อ "สวนผักลุงมี" เบอร์ 081-234-5678 เพิ่มเข้าระบบแล้วผูกผักร็อกเก็ตให้เจ้านี้ |
| SC-011 | staff-branch | desktop | `['/']` | ฝากบอกกะบ่ายว่าของ FOODGALLERY จะมาบ่ายนี้ ให้รอรับด้วย |
| SC-012 | staff-branch | desktop | `['/products', '/issue']` | กล่องชีสมีบาร์โค้ด 8851234567890 อยากให้สแกนแล้วขึ้นมาเลยตอนเบิก ทำให้ใช้ได้ แล้วลองเบิก 1 กิโลด้วยวิธีนั้น |
| SC-013 | manager | desktop | `['/import']` | มีไฟล์ Excel ยอดปิดสิ้นเดือนจากสาขา อยากเอาตัวเลขเข้าระบบทีเดียวทั้งไฟล์ |
| SC-014 | manager | desktop | `['/purchase', '/purchase/import', '/purchase/:id']` | มีไฟล์ Excel รายการที่ต้องสั่งประจำสัปดาห์ อยากให้ระบบแยกใบสั่งซื้อตามผู้ขายให้เอง แล้วส่งให้ผู้ขายทาง LINE |
| SC-015 | manager | desktop | `['/orders']` | ใบสั่งซื้อที่ส่งไปเมื่อเช้า ผู้ขายบอกว่าของหมด ต้องลดจำนวนลงครึ่งหนึ่ง แก้แล้วบอกผู้ขายใหม่ |
| SC-016 | manager | desktop | `['/settings']` | ตอนคีย์รับของ หน่วยที่เลือกได้ไม่มี "ถุง" อยากเพิ่มเข้าไป |

SC-013 and SC-014 need a spreadsheet. The run uses any `.xlsx` the agent builds itself with two columns; **do not copy anything from `samples/`**, that folder is gitignored company data.

SC-016 exists to find out what the app does when a manager wants something only an admin can change. Being refused is a valid outcome; being refused **without being told why or who to ask** is a finding.

- [ ] **Step 3: Verify every route in the app appears in at least one scenario**

Run:
```bash
cd pizza-stock && grep -o 'path="[^"]*"' src/App.tsx | sed 's/path="//;s/"//' | grep -v '^\*$' | sort > /tmp/routes.txt && grep -ho "routes: \[.*\]" docs/ux-lab/scenarios/*.md | tr -d "'[]" | sed 's/routes: //' | tr ',' '\n' | sed 's/^ *//' | sort -u > /tmp/covered.txt && comm -23 /tmp/routes.txt /tmp/covered.txt
```
Expected: prints only `/more` and `/settings/:section` — both are navigation shells reached while doing the scenarios above, not destinations of their own. Anything else printed is a gap: add it to a scenario's `routes` or write another scenario.

- [ ] **Step 4: Commit**

```bash
git add docs/ux-lab/scenarios
git commit -m "UX lab: sixteen goals covering every page, none of them naming a button"
```

---

## Task 4: The run protocol and report template

**Files:**
- Create: `docs/ux-lab/runs/TEMPLATE.md`
- Modify: `docs/ux-lab/README.md` (append a `## วิธีรัน` section)

**Interfaces:**
- Consumes: scenario ids, persona ids.
- Produces: the report shape every run folder follows, and the seven metric names that Task 7 reads when filing issues.

- [ ] **Step 1: Write `runs/TEMPLATE.md`**

```markdown
---
scenario: SC-000
persona: staff-branch
viewport: desktop
startedAt: 2026-09-23T10:00:00+07:00
endedAt: 2026-09-23T10:06:30+07:00
outcome: completed | completed_with_help | abandoned
metrics:
  completionSeconds: 0
  meaningfulActions: 0
  searchFailures: 0
  validationErrors: 0
  backNavigations: 0
  deadEnds: 0
---

# SC-000 — <ชื่อ scenario>

## สิ่งที่ตั้งใจจะทำ

<เป้าหมายจาก scenario คัดลอกมาตรง ๆ>

## เกิดอะไรขึ้นจริง

<เล่าตามลำดับเวลา บอกทุกครั้งที่เดาผิด กดผิด หรือหยุดคิดนาน
เขียนเป็นภาษาที่พนักงานพูด ไม่ใช่ภาษาโปรแกรมเมอร์>

## จุดที่ติด

### 1. <หัวข้อสั้น ๆ>

- **คาดว่าจะเป็น:** <สิ่งที่คิดว่าจะเกิด>
- **เกิดจริง:** <สิ่งที่เกิด>
- **หน้า:** <route>
- **ภาพ:** shots/01-<slug>.png

## ข้อสังเกตอื่น

<อะไรที่ไม่ถึงขั้นเป็นปัญหา แต่ควรบันทึก>
```

- [ ] **Step 2: Append `## วิธีรัน` to the README**

The section states, as a numbered list:

1. เปิดสนามซ้อม: `npm run demo` แล้วเปิด `http://localhost:5175`
2. **กด "รีเซ็ตข้อมูลเดโม" ก่อนทุก run** เพื่อให้ทุก run เริ่มจากจุดเดียวกัน (บัญชีเดโมคือ `demo@inventory-pzm.local` / `demo1234` — อยู่ในหน้าเข้าสู่ระบบของเดโมอยู่แล้ว)
3. ตั้งขนาดจอ: `desktop` = 1440×900, `mobile` = 375×812
4. อ่าน persona แล้วอ่าน scenario — **แล้วปิดสเปกทิ้ง** ห้ามเปิดโค้ดหรือเอกสารอื่นระหว่างรัน
5. ทำงานให้บรรลุเป้าหมาย จับเวลาตั้งแต่หน้าแรกหลังเข้าสู่ระบบ
6. ถ่ายภาพทุกครั้งที่ติด
7. เขียน `report.md` จาก `TEMPLATE.md`

And this rule in bold: **ถ้าระหว่างรันเผลอไปอ่านโค้ดเพื่อหาว่าปุ่มอยู่ไหน ให้ทิ้ง run นั้นแล้วเริ่มใหม่ — run นั้นใช้ไม่ได้แล้ว**

- [ ] **Step 3: Commit**

```bash
git add docs/ux-lab/runs/TEMPLATE.md docs/ux-lab/README.md
git commit -m "UX lab: how a run is conducted and what it must record"
```

---

## Task 5: First desktop sweep

**Files:**
- Create: `docs/ux-lab/runs/2026-09-23-SC-0NN-<persona>-desktop/report.md` (one folder per scenario)
- Create: `docs/ux-lab/runs/.../shots/*.png`

**Interfaces:**
- Consumes: everything from Tasks 1–4.
- Produces: run folder names, which Task 7's issues cite as evidence in their `runs: []` field.

- [ ] **Step 1: Start the demo server**

Create `.claude/launch.json` if it does not exist:

```json
{
  "version": "0.0.1",
  "configurations": [
    { "name": "demo", "runtimeExecutable": "npm", "runtimeArgs": ["run", "demo"], "port": 5175 }
  ]
}
```

Then start it with the preview tool (`preview_start` with `{name: "demo"}`), not with Bash.

- [ ] **Step 2: Reset the demo and set the viewport**

Sign in as `demo@inventory-pzm.local` / `demo1234`, press "รีเซ็ตข้อมูลเดโม", let the page reload. Set the viewport to 1440×900 (`resize_window` with `{width: 1440, height: 900}`).

- [ ] **Step 3: Run SC-001 through SC-016 in order, one at a time**

For each scenario: re-read only that scenario file and its persona, work the app to the goal, take a screenshot at every point of confusion, then write `report.md` from the template with all seven metrics filled in.

`completionSeconds` is wall-clock from the first click after sign-in to the moment the "ถือว่าเสร็จเมื่อ" condition holds. If it never holds, `outcome: abandoned` and record the seconds spent before giving up.

- [ ] **Step 4: Verify every run folder is complete**

Run:
```bash
cd pizza-stock && for d in docs/ux-lab/runs/2026-09-23-*/; do test -f "$d/report.md" || echo "MISSING report: $d"; grep -q "outcome:" "$d/report.md" || echo "MISSING outcome: $d"; done; echo done
```
Expected: `done` with no `MISSING` lines.

- [ ] **Step 5: Commit**

```bash
git add docs/ux-lab/runs
git commit -m "UX lab: first desktop sweep, sixteen scenarios worked by agents"
```

---

## Task 6: Ledger, state machine and coverage checks

**Files:**
- Create: `scripts/ux-lab/ledger.ts`
- Create: `scripts/ux-lab/coverage.ts`
- Create: `scripts/ux-lab/cli.mjs`
- Create: `scripts/tsconfig.json`
- Create: `tests/ux-lab-ledger.test.ts`
- Create: `tests/ux-lab-coverage.test.ts`
- Modify: `package.json` (add the `ux-lab` script)

**Interfaces:**
- Consumes: nothing at runtime; reads `docs/ux-lab/` from disk in the CLI only.
- Produces: `LedgerIssue`, `Ledger`, `IssueState`, `Category`, `Severity`, `Risk`, `NEXT_STATES`, `canTransition(from, to)`, `nextIssueId(ledger)`, `dedupeKey(issue)`, `findDuplicate(ledger, candidate)`, `validateLedger(ledger, issueFiles)`, `missingRoutes(appRoutes, scenarioRoutes)`.

- [ ] **Step 1: Write the failing tests for `ledger.ts`**

`tests/ux-lab-ledger.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import {
  canTransition,
  dedupeKey,
  findDuplicate,
  nextIssueId,
  validateLedger,
  type Ledger,
  type LedgerIssue,
} from '../scripts/ux-lab/ledger'

const issue = (over: Partial<LedgerIssue> = {}): LedgerIssue => ({
  id: 'UX-0001',
  title: 'ค้นหาสินค้าด้วยชื่อเล่นไม่เจอ',
  file: 'issues/UX-0001-search-nickname.md',
  role: 'staff',
  route: '/requests',
  category: 'UX_FRICTION',
  severity: 'major',
  risk: 'MEDIUM',
  state: 'NEW',
  frequency: 1,
  runs: ['2026-09-23-SC-001-staff-branch-desktop'],
  ...over,
})

const ledger = (issues: LedgerIssue[]): Ledger => ({ version: 1, updatedAt: '2026-09-23', issues })

describe('canTransition', () => {
  it('walks the happy path all the way to CLOSED', () => {
    const path = [
      'NEW', 'ANALYZING', 'REPRODUCED', 'FIXING', 'QA_PASSED',
      'READY_FOR_USER_RETEST', 'VERIFIED', 'READY_FOR_RELEASE', 'RELEASED', 'CLOSED',
    ] as const
    for (let i = 0; i < path.length - 1; i++) {
      expect(canTransition(path[i], path[i + 1])).toBe(true)
    }
  })

  it('lets a failed QA go back to FIXING and a failed retest go back to ANALYZING', () => {
    expect(canTransition('FIXING', 'QA_FAILED')).toBe(true)
    expect(canTransition('QA_FAILED', 'FIXING')).toBe(true)
    expect(canTransition('READY_FOR_USER_RETEST', 'ANALYZING')).toBe(true)
  })

  it('lets analysis close an issue that needs no code change', () => {
    expect(canTransition('ANALYZING', 'CLOSED')).toBe(true)
  })

  it('refuses to jump the queue', () => {
    expect(canTransition('NEW', 'FIXING')).toBe(false)
    expect(canTransition('REPRODUCED', 'RELEASED')).toBe(false)
    expect(canTransition('CLOSED', 'NEW')).toBe(false)
  })
})

describe('nextIssueId', () => {
  it('starts at UX-0001 on an empty ledger', () => {
    expect(nextIssueId(ledger([]))).toBe('UX-0001')
  })

  it('carries on from the highest number, not the count', () => {
    expect(nextIssueId(ledger([issue({ id: 'UX-0001' }), issue({ id: 'UX-0007' })]))).toBe('UX-0008')
  })
})

describe('dedupeKey', () => {
  it('groups by the page and the kind of problem', () => {
    expect(dedupeKey(issue())).toBe('/requests|UX_FRICTION')
  })

  it('finds an existing issue the same run would have filed twice', () => {
    const l = ledger([issue()])
    expect(findDuplicate(l, issue({ id: 'UX-0002' }))?.id).toBe('UX-0001')
  })

  it('does not merge the same page with a different kind of problem', () => {
    const l = ledger([issue()])
    expect(findDuplicate(l, issue({ id: 'UX-0002', category: 'BUG' }))).toBeUndefined()
  })
})

describe('validateLedger', () => {
  const files = ['issues/UX-0001-search-nickname.md']

  it('passes a ledger that agrees with the files on disk', () => {
    expect(validateLedger(ledger([issue()]), files)).toEqual([])
  })

  it('catches an issue whose file is missing', () => {
    const problems = validateLedger(ledger([issue({ file: 'issues/UX-0001-gone.md' })]), files)
    expect(problems.join(' ')).toContain('UX-0001')
  })

  it('catches a file on disk that no ledger entry claims', () => {
    const problems = validateLedger(ledger([]), files)
    expect(problems.join(' ')).toContain('UX-0001-search-nickname.md')
  })

  it('catches two issues sharing an id', () => {
    const problems = validateLedger(ledger([issue(), issue({ file: 'issues/UX-0001-search-nickname.md' })]), files)
    expect(problems.join(' ')).toContain('ซ้ำ')
  })

  it('catches frequency that disagrees with the runs listed', () => {
    const problems = validateLedger(ledger([issue({ frequency: 4 })]), files)
    expect(problems.join(' ')).toContain('frequency')
  })

  // The owner's safety rule, made mechanical: a risky change cannot be recorded as
  // released unless the owner's approval was recorded with it.
  it('refuses a HIGH issue that reached RELEASED without the owner approving', () => {
    const problems = validateLedger(ledger([issue({ risk: 'HIGH', state: 'RELEASED' })]), files)
    expect(problems.join(' ')).toContain('ownerApproved')
  })

  it('accepts a HIGH issue released with approval recorded', () => {
    expect(validateLedger(ledger([issue({ risk: 'HIGH', state: 'RELEASED', ownerApproved: true })]), files)).toEqual([])
  })

  it('never lets a CRITICAL issue be released automatically', () => {
    const problems = validateLedger(ledger([issue({ risk: 'CRITICAL', state: 'RELEASED' })]), files)
    expect(problems.join(' ')).toContain('ownerApproved')
  })

  it('catches a dupeOf pointing at nothing', () => {
    const problems = validateLedger(ledger([issue({ dupeOf: 'UX-9999' })]), files)
    expect(problems.join(' ')).toContain('UX-9999')
  })
})
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `cd pizza-stock && npx vitest run tests/ux-lab-ledger.test.ts`
Expected: FAIL — `Failed to resolve import "../scripts/ux-lab/ledger"`.

- [ ] **Step 3: Write `scripts/ux-lab/ledger.ts`**

```ts
// The issue ledger: what an issue is, which moves it may make, and whether the index on
// disk still agrees with the files beside it.
//
// This module is deliberately pure. The CLI reads the files; everything decided here is
// decided from values, so the rules that matter — above all that a risky change cannot be
// recorded as released without the owner's approval — are covered by tests rather than by
// someone remembering them.

export type IssueState =
  | 'NEW'
  | 'ANALYZING'
  | 'REPRODUCED'
  | 'NEEDS_MORE_DATA'
  | 'FIXING'
  | 'QA_PASSED'
  | 'QA_FAILED'
  | 'READY_FOR_USER_RETEST'
  | 'VERIFIED'
  | 'READY_FOR_RELEASE'
  | 'RELEASED'
  | 'CLOSED'

export type Category =
  | 'BUG'
  | 'UX_FRICTION'
  | 'MISSING_FEATURE'
  | 'BUSINESS_RULE'
  | 'TRAINING_ISSUE'
  | 'PERFORMANCE'

export type Severity = 'blocker' | 'major' | 'minor' | 'cosmetic'

/** How dangerous the fix is, which is not the same question as how much the bug hurts. */
export type Risk = 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'

export interface LedgerIssue {
  id: string
  title: string
  /** Path of the issue's own file, relative to docs/ux-lab/. */
  file: string
  role: 'staff' | 'manager'
  route: string
  category: Category
  severity: Severity
  risk: Risk
  state: IssueState
  /** How many runs hit this. Must equal runs.length. */
  frequency: number
  runs: string[]
  /** Set when this turned out to be another issue wearing a different hat. */
  dupeOf?: string
  /** The owner said yes, in chat, to this reaching production. */
  ownerApproved?: boolean
}

export interface Ledger {
  version: 1
  updatedAt: string
  issues: LedgerIssue[]
}

export const NEXT_STATES: Record<IssueState, IssueState[]> = {
  NEW: ['ANALYZING'],
  // Analysis may end the story: a TRAINING_ISSUE is not a code change.
  ANALYZING: ['REPRODUCED', 'NEEDS_MORE_DATA', 'CLOSED'],
  NEEDS_MORE_DATA: ['ANALYZING'],
  REPRODUCED: ['FIXING'],
  FIXING: ['QA_PASSED', 'QA_FAILED'],
  QA_FAILED: ['FIXING'],
  QA_PASSED: ['READY_FOR_USER_RETEST'],
  // A retest that still fails sends it back to the start of thinking, not of fixing.
  READY_FOR_USER_RETEST: ['VERIFIED', 'ANALYZING'],
  VERIFIED: ['READY_FOR_RELEASE'],
  READY_FOR_RELEASE: ['RELEASED'],
  RELEASED: ['CLOSED'],
  CLOSED: [],
}

export function canTransition(from: IssueState, to: IssueState): boolean {
  return NEXT_STATES[from].includes(to)
}

const NUMBER = /^UX-(\d{4})$/

export function nextIssueId(ledger: Ledger): string {
  const highest = ledger.issues.reduce((max, i) => {
    const m = NUMBER.exec(i.id)
    return m ? Math.max(max, Number(m[1])) : max
  }, 0)
  return `UX-${String(highest + 1).padStart(4, '0')}`
}

/** Two findings on the same page about the same kind of problem are one issue. */
export function dedupeKey(issue: Pick<LedgerIssue, 'route' | 'category'>): string {
  return `${issue.route}|${issue.category}`
}

export function findDuplicate(ledger: Ledger, candidate: Pick<LedgerIssue, 'route' | 'category'>): LedgerIssue | undefined {
  const key = dedupeKey(candidate)
  return ledger.issues.find((i) => !i.dupeOf && dedupeKey(i) === key)
}

/** States in which a change has actually reached the company's real data. */
const RELEASED_STATES: IssueState[] = ['RELEASED', 'CLOSED']

export function validateLedger(ledger: Ledger, issueFiles: readonly string[]): string[] {
  const problems: string[] = []
  const seen = new Set<string>()
  const ids = new Set(ledger.issues.map((i) => i.id))
  const claimed = new Set<string>()

  for (const i of ledger.issues) {
    if (!NUMBER.test(i.id)) problems.push(`${i.id}: รหัสต้องเป็นรูปแบบ UX-0001`)
    if (seen.has(i.id)) problems.push(`${i.id}: รหัสซ้ำในทะเบียน`)
    seen.add(i.id)
    claimed.add(i.file)

    if (!issueFiles.includes(i.file)) problems.push(`${i.id}: ทะเบียนอ้างไฟล์ ${i.file} ที่ไม่มีอยู่จริง`)
    if (i.frequency !== i.runs.length) {
      problems.push(`${i.id}: frequency = ${i.frequency} แต่มี run อยู่ ${i.runs.length} รายการ`)
    }
    if (i.dupeOf && !ids.has(i.dupeOf)) problems.push(`${i.id}: dupeOf ชี้ไปที่ ${i.dupeOf} ซึ่งไม่มีในทะเบียน`)
    if ((i.risk === 'HIGH' || i.risk === 'CRITICAL') && RELEASED_STATES.includes(i.state) && !i.ownerApproved) {
      problems.push(`${i.id}: risk ${i.risk} ขึ้นของจริงแล้วแต่ไม่มี ownerApproved — ต้องให้เจ้าของอนุมัติก่อน`)
    }
  }

  for (const file of issueFiles) {
    if (!claimed.has(file)) problems.push(`${file}: มีไฟล์อยู่แต่ไม่มีในทะเบียน`)
  }

  return problems
}
```

- [ ] **Step 4: Run the tests to verify they pass**

Run: `cd pizza-stock && npx vitest run tests/ux-lab-ledger.test.ts`
Expected: PASS, 18 tests.

- [ ] **Step 5: Write the failing test for `coverage.ts`**

`tests/ux-lab-coverage.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { missingRoutes } from '../scripts/ux-lab/coverage'

describe('missingRoutes', () => {
  it('is empty when every route is reached by some scenario', () => {
    expect(missingRoutes(['/', '/products'], [['/'], ['/products']])).toEqual([])
  })

  it('names the routes nobody visits', () => {
    expect(missingRoutes(['/', '/products', '/reports'], [['/', '/products']])).toEqual(['/reports'])
  })

  it('ignores the catch-all route', () => {
    expect(missingRoutes(['/', '*'], [['/']])).toEqual([])
  })

  it('treats the navigation shells as always covered', () => {
    expect(missingRoutes(['/', '/more', '/settings/:section'], [['/']])).toEqual([])
  })
})
```

- [ ] **Step 6: Run it to verify it fails**

Run: `cd pizza-stock && npx vitest run tests/ux-lab-coverage.test.ts`
Expected: FAIL — `Failed to resolve import "../scripts/ux-lab/coverage"`.

- [ ] **Step 7: Write `scripts/ux-lab/coverage.ts`**

```ts
// Which pages of the app no scenario ever asks anyone to visit.
//
// A page nobody has a reason to open is a page whose problems nobody will report, so this
// is checked rather than assumed.

/**
 * Reached while doing something else rather than as a destination: the catch-all redirect,
 * the phone's overflow menu, and the settings page's own sub-routes.
 */
const NOT_DESTINATIONS = ['*', '/more', '/settings/:section']

export function missingRoutes(appRoutes: readonly string[], scenarioRoutes: readonly (readonly string[])[]): string[] {
  const covered = new Set(scenarioRoutes.flat())
  return appRoutes.filter((r) => !NOT_DESTINATIONS.includes(r) && !covered.has(r))
}
```

- [ ] **Step 8: Run it to verify it passes**

Run: `cd pizza-stock && npx vitest run tests/ux-lab-coverage.test.ts`
Expected: PASS, 4 tests.

- [ ] **Step 9: Write `scripts/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "es2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "lib": ["es2022"],
    "types": ["node"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "noUnusedLocals": true
  },
  "include": ["ux-lab/**/*.ts"]
}
```

- [ ] **Step 10: Write `scripts/ux-lab/cli.mjs`**

```js
// Checks that the lab's index still describes the lab.
//
//   npm run ux-lab
//
// Reads docs/ux-lab/ledger.json, the issue files beside it, the scenarios' routes and the
// routes the app actually defines, then reports everything that disagrees. Exits 1 on any
// problem so the gate can depend on it.

import { readFileSync, readdirSync, existsSync } from 'node:fs'
import { join } from 'node:path'
import { fileURLToPath } from 'node:url'
import { validateLedger } from './ledger.ts'
import { missingRoutes } from './coverage.ts'

const LAB = fileURLToPath(new URL('../../docs/ux-lab/', import.meta.url))
const APP = fileURLToPath(new URL('../../src/App.tsx', import.meta.url))

const ledger = JSON.parse(readFileSync(join(LAB, 'ledger.json'), 'utf8'))

const issuesDir = join(LAB, 'issues')
const issueFiles = existsSync(issuesDir)
  ? readdirSync(issuesDir).filter((f) => f.endsWith('.md')).map((f) => `issues/${f}`)
  : []

const appRoutes = [...readFileSync(APP, 'utf8').matchAll(/path="([^"]*)"/g)].map((m) => m[1])

const scenarioRoutes = readdirSync(join(LAB, 'scenarios'))
  .filter((f) => f.startsWith('SC-') && f.endsWith('.md'))
  .map((f) => {
    const text = readFileSync(join(LAB, 'scenarios', f), 'utf8')
    const line = /^routes:\s*\[(.*)\]\s*$/m.exec(text)
    if (!line) throw new Error(`${f}: ไม่มีบรรทัด routes: [...] ใน front-matter`)
    return line[1].split(',').map((s) => s.trim().replace(/^['"]|['"]$/g, '')).filter(Boolean)
  })

const problems = [
  ...validateLedger(ledger, issueFiles),
  ...missingRoutes(appRoutes, scenarioRoutes).map((r) => `${r}: ไม่มี scenario ไหนพาไปหน้านี้`),
]

if (problems.length === 0) {
  console.log(`\n✓ ทะเบียน ${ledger.issues.length} เรื่อง ตรงกับไฟล์ และทุกหน้ามี scenario ครอบ\n`)
  process.exit(0)
}

console.error(`\n✗ พบ ${problems.length} จุดที่ไม่ตรงกัน:\n`)
for (const p of problems) console.error(`  - ${p}`)
console.error('')
process.exit(1)
```

- [ ] **Step 11: Create the empty ledger and add the npm script**

`docs/ux-lab/ledger.json`:

```json
{
  "version": 1,
  "updatedAt": "2026-09-23",
  "issues": []
}
```

In `package.json`, add to `scripts`, immediately after the `"i18n:check"` line:

```json
    "ux-lab": "node --experimental-strip-types scripts/ux-lab/cli.mjs",
```

- [ ] **Step 12: Run the CLI and the typecheck**

Run:
```bash
cd pizza-stock && npm run ux-lab && npx tsc -p scripts/tsconfig.json --noEmit && echo TYPECHECK_OK
```
Expected: `✓ ทะเบียน 0 เรื่อง ตรงกับไฟล์ และทุกหน้ามี scenario ครอบ`, then `TYPECHECK_OK`.

- [ ] **Step 13: Run the full gate**

Run:
```bash
cd pizza-stock && npx tsc -p tsconfig.app.json --noEmit && npm run lint && npm run i18n:check && npm test && npx tsc -p worker/tsconfig.json --noEmit && npm run build
```
Expected: all pass; `npm test` now reports **701 passed** (679 + 18 + 4).

- [ ] **Step 14: Commit**

```bash
git add scripts/ux-lab scripts/tsconfig.json tests/ux-lab-ledger.test.ts tests/ux-lab-coverage.test.ts docs/ux-lab/ledger.json package.json
git commit -m "UX lab: a ledger that refuses to say a risky fix shipped without approval"
```

---

## Task 7: Turn the sweep into issues

**Files:**
- Create: `docs/ux-lab/issues/UX-NNNN-<slug>.md` (one per deduped finding)
- Modify: `docs/ux-lab/ledger.json`

**Interfaces:**
- Consumes: run reports from Task 5; `nextIssueId`, `findDuplicate`, `dedupeKey` from Task 6.
- Produces: issue ids that Task 8 works from.

- [ ] **Step 1: Collect every "จุดที่ติด" from every run report**

List them with their route and the run folder they came from. Do not write files yet.

- [ ] **Step 2: Dedupe**

Group by `route|category` exactly as `dedupeKey` does. Findings that land in the same group become one issue whose `runs` lists every run that hit it and whose `frequency` equals that count. **Keep every run's evidence — never drop one because another already showed the same thing.**

- [ ] **Step 3: Write one issue file per group**

Shape, with front-matter matching the ledger entry exactly:

```markdown
---
id: UX-0001
title: ค้นหาสินค้าด้วยชื่อเล่นไม่เจอ
role: staff
route: /requests
category: UX_FRICTION
severity: major
risk: MEDIUM
state: NEW
frequency: 2
runs:
  - 2026-09-23-SC-001-staff-branch-desktop
  - 2026-09-23-SC-012-staff-branch-desktop
---

# ค้นหาสินค้าด้วยชื่อเล่นไม่เจอ

## อาการ

<สิ่งที่เกิดขึ้นจริง เขียนจากมุมผู้ใช้>

## ที่คาดว่าจะเป็น

<สิ่งที่ผู้ใช้คิดว่าจะเกิด>

## ทำซ้ำได้โดย

1. <ขั้นตอนที่สั้นที่สุดที่ทำให้เกิดอาการ>

## หลักฐาน

- `runs/2026-09-23-SC-001-staff-branch-desktop/report.md` §จุดที่ติด 1
- `runs/2026-09-23-SC-001-staff-branch-desktop/shots/01-search.png`

## ทำไมจึงจัดเป็น <category> / risk <risk>

<เหตุผลสั้น ๆ — ข้อนี้กันการยกระดับความเสี่ยงโดยไม่ตั้งใจ>
```

Assign `category` honestly. If the app behaved correctly and the persona simply did not understand it, that is `TRAINING_ISSUE` and **no code will be changed for it**.

Assign `risk` from the Global Constraints table by what the *fix* would touch, not by how annoying the bug is.

- [ ] **Step 4: Write the ledger entries**

Add one object per issue to `docs/ux-lab/ledger.json` with exactly the fields in `LedgerIssue`, and set `updatedAt` to today.

- [ ] **Step 5: Verify the index and the files agree**

Run: `cd pizza-stock && npm run ux-lab`
Expected: `✓ ทะเบียน N เรื่อง ตรงกับไฟล์ และทุกหน้ามี scenario ครอบ`

- [ ] **Step 6: Commit**

```bash
git add docs/ux-lab/issues docs/ux-lab/ledger.json
git commit -m "UX lab: the desktop sweep's findings, deduped into issues"
```

---

## Task 8: Walk one issue all the way to VERIFIED

**Files:**
- Modify: the app files the chosen issue actually implicates
- Create: a regression test under `tests/`
- Modify: the chosen `docs/ux-lab/issues/UX-NNNN-*.md` and `docs/ux-lab/ledger.json` (state changes)
- Create: `docs/ux-lab/runs/<date>-SC-0NN-<persona>-desktop-retest/report.md`

**Interfaces:**
- Consumes: issues from Task 7; `canTransition` from Task 6.
- Produces: the worked example every later fix follows.

- [ ] **Step 1: Choose the issue**

Pick the highest `severity` among issues with `risk` of `LOW` or `MEDIUM`. Do **not** pick a `HIGH` or `CRITICAL` issue for this first pass — those need the owner in the loop and this task is about proving the machinery. Do not pick a `TRAINING_ISSUE`.

Set its state to `ANALYZING` in both the issue file and the ledger.

- [ ] **Step 2: Reproduce it in the demo**

Follow the issue's own "ทำซ้ำได้โดย" steps in the demo build. If the symptom appears, set state to `REPRODUCED`. If it does not, set state to `NEEDS_MORE_DATA`, write down what differed, and **stop — do not edit app code**. Pick the next issue and start Step 1 again.

- [ ] **Step 3: Write the failing regression test**

Before touching any app file, write a test that fails for the reason the issue describes. Put it in the existing test file for that area if one exists (`tests/search.test.ts`, `tests/products.test.ts`, `tests/purchase-requests.test.ts`, …), otherwise create a new one.

Example shape, for the search case the owner's spec names. The matcher already in the
codebase is `looseMatch(fields, needle)` from `src/lib/search.ts` — use the real signature:

```ts
import { looseMatch } from '../src/lib/search'

it('matches a product from the part of the name a person actually says', () => {
  const fields = ['RED OAK SALAD (ACK)', 'VGT-01-08-101']
  for (const term of ['red oak', 'RED OAK', 'oak', 'VGT-01-08-101']) {
    expect(looseMatch(fields, term)).toBe(true)
  }
})
```

- [ ] **Step 4: Run it and watch it fail for the right reason**

Run: `cd pizza-stock && npx vitest run tests/<file>.test.ts`
Expected: FAIL, and the failure message describes the issue's symptom — not a typo or a missing import. If it fails for another reason, fix the test first.

Set state to `FIXING`.

- [ ] **Step 5: Write the smallest fix that makes it pass**

Change only what the root cause requires. **Do not** rewrite the file, rename things nearby, or "tidy up while in there". If the fix would change supplier mapping, approved quantity, approval state or purchase-order generation, stop: that is `HIGH` and belongs to the owner.

- [ ] **Step 6: Run the test**

Run: `cd pizza-stock && npx vitest run tests/<file>.test.ts`
Expected: PASS.

- [ ] **Step 7: Run the whole gate**

Run:
```bash
cd pizza-stock && npx tsc -p tsconfig.app.json --noEmit && npm run lint && npm run i18n:check && npm test && npm run test:rules && npx tsc -p worker/tsconfig.json --noEmit && npm run build && npm run ux-lab
```
Expected: every command exits 0.

Any failure means state `QA_FAILED` and back to Step 5. **Do not proceed with a red gate.**

On success, set state to `QA_PASSED`.

- [ ] **Step 8: Commit the fix**

```bash
git add -A
git commit -m "Fix: <what a user would say was wrong>, with the test that keeps it fixed"
```

- [ ] **Step 9: Put it on the staging branch**

```bash
cd pizza-stock && BR=$(git branch --show-current) && git checkout demo && git merge --no-ff "$BR" -m "UX lab fix to staging" && git push origin demo && git checkout "$BR"
```

Wait for the Cloudflare build, then confirm the deploy landed:

```bash
curl -s https://demo.pzmstock.pages.dev/ | grep -o 'assets/index-[A-Za-z0-9_-]*\.js'
```
Expected: the same hash as the local `dist/assets/index-*.js`.

Set state to `READY_FOR_USER_RETEST`.

- [ ] **Step 10: Retest with the same persona and the same scenario**

Run the original scenario again, same viewport, from a fresh demo reset. Write a new run report into a folder whose name ends `-retest`. Fill in all seven metrics.

Add a `## ผลการ retest` section to the issue file with the before/after table:

```markdown
| ตัววัด | ก่อนแก้ | หลังแก้ |
|---|---|---|
| completionSeconds | 0 | 0 |
| meaningfulActions | 0 | 0 |
| searchFailures | 0 | 0 |
```

If the numbers improved and the goal was reached, set state to `VERIFIED`. If not, set state to `ANALYZING` and start again from Step 2 — **do not** declare it verified because the code changed.

- [ ] **Step 11: Commit and report to the owner**

```bash
git add -A
git commit -m "UX lab: <issue id> verified on staging"
```

Then tell the owner what was fixed, what the before/after numbers were, and ask whether to release it. **Do not merge to `main`.**

---

## Task 9: Mobile sweep

**Files:**
- Create: `docs/ux-lab/runs/<date>-SC-0NN-<persona>-mobile/` (one per scenario)
- Modify: `docs/ux-lab/issues/*.md` and `docs/ux-lab/ledger.json` (new issues, and `frequency`/`runs` on existing ones)

**Interfaces:**
- Consumes: everything above.
- Produces: the second half of the evidence, and the issues that only exist on a phone.

- [ ] **Step 1: Set the viewport to the phone shell**

`resize_window` with `{preset: "mobile"}` (375×812), then reload the page so the load-time device checks re-run.

- [ ] **Step 2: Run SC-001 through SC-016 again, resetting the demo before each**

Same protocol as Task 5. The phone shell is a different code path — tab bar, cards instead of tables, `QtySheet` instead of inline inputs — so expect findings that have no desktop equivalent.

- [ ] **Step 3: Fold the findings into the ledger**

A finding that matches an existing issue's `route|category` adds a run to it and increases `frequency`. A finding that only happens on the phone is a new issue; say so in its "ทำซ้ำได้โดย" section.

- [ ] **Step 4: Verify**

Run: `cd pizza-stock && npm run ux-lab`
Expected: `✓ …` with no problems.

- [ ] **Step 5: Commit**

```bash
git add docs/ux-lab
git commit -m "UX lab: mobile sweep, and what only breaks on a phone"
```

---

## Task 10: The status page

**Files:**
- Create: `scripts/ux-lab/report.ts`
- Create: `tests/ux-lab-report.test.ts`
- Modify: `scripts/ux-lab/cli.mjs` (add the `report` argument)
- Modify: `.gitignore` (ignore the generated HTML)

**Interfaces:**
- Consumes: `Ledger`, `LedgerIssue` from Task 6.
- Produces: `summarise(ledger)` and `renderReport(ledger)`.

- [ ] **Step 1: Write the failing test**

`tests/ux-lab-report.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { renderReport, summarise } from '../scripts/ux-lab/report'
import type { Ledger, LedgerIssue } from '../scripts/ux-lab/ledger'

const issue = (over: Partial<LedgerIssue> = {}): LedgerIssue => ({
  id: 'UX-0001',
  title: 'ค้นหาไม่เจอ',
  file: 'issues/UX-0001-a.md',
  role: 'staff',
  route: '/requests',
  category: 'UX_FRICTION',
  severity: 'major',
  risk: 'MEDIUM',
  state: 'NEW',
  frequency: 1,
  runs: ['r1'],
  ...over,
})

const ledger = (issues: LedgerIssue[]): Ledger => ({ version: 1, updatedAt: '2026-09-23', issues })

describe('summarise', () => {
  it('counts the open work by state', () => {
    const s = summarise(ledger([issue(), issue({ id: 'UX-0002', state: 'FIXING' }), issue({ id: 'UX-0003', state: 'NEW' })]))
    expect(s.byState.NEW).toBe(2)
    expect(s.byState.FIXING).toBe(1)
  })

  it('counts a state nobody is in as zero rather than leaving it out', () => {
    expect(summarise(ledger([])).byState.VERIFIED).toBe(0)
  })

  it('puts the most frequent issues first', () => {
    const s = summarise(ledger([issue({ frequency: 1, runs: ['r1'] }), issue({ id: 'UX-0002', frequency: 3, runs: ['r1', 'r2', 'r3'] })]))
    expect(s.ranked[0].id).toBe('UX-0002')
  })

  it('does not count issues that turned out to be duplicates', () => {
    expect(summarise(ledger([issue(), issue({ id: 'UX-0002', dupeOf: 'UX-0001' })])).open).toBe(1)
  })
})

describe('renderReport', () => {
  it('escapes a title that contains HTML', () => {
    const html = renderReport(ledger([issue({ title: '<script>alert(1)</script>' })]))
    expect(html).not.toContain('<script>alert(1)</script>')
    expect(html).toContain('&lt;script&gt;')
  })

  it('shows every issue id', () => {
    const html = renderReport(ledger([issue(), issue({ id: 'UX-0002' })]))
    expect(html).toContain('UX-0001')
    expect(html).toContain('UX-0002')
  })
})
```

- [ ] **Step 2: Run it to verify it fails**

Run: `cd pizza-stock && npx vitest run tests/ux-lab-report.test.ts`
Expected: FAIL — `Failed to resolve import "../scripts/ux-lab/report"`.

- [ ] **Step 3: Write `scripts/ux-lab/report.ts`**

```ts
// The status page the owner's spec calls a "UX Improvement Center".
//
// It is generated from the ledger rather than served by the app: the people who need it
// are the owner and whoever is fixing things, and neither needs it inside the warehouse
// app. Generating it also means it cannot drift from the files — there is one source.

import { NEXT_STATES, type Ledger, type IssueState, type LedgerIssue } from './ledger.ts'

export interface Summary {
  byState: Record<IssueState, number>
  ranked: LedgerIssue[]
  open: number
}

const LIVE_STATES: IssueState[] = ['RELEASED', 'CLOSED']

export function summarise(ledger: Ledger): Summary {
  const byState = Object.fromEntries(
    (Object.keys(NEXT_STATES) as IssueState[]).map((s) => [s, 0]),
  ) as Record<IssueState, number>

  const real = ledger.issues.filter((i) => !i.dupeOf)
  for (const i of real) byState[i.state]++

  return {
    byState,
    ranked: [...real].sort((a, b) => b.frequency - a.frequency || a.id.localeCompare(b.id)),
    open: real.filter((i) => !LIVE_STATES.includes(i.state)).length,
  }
}

const escape = (s: string) =>
  s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;')

export function renderReport(ledger: Ledger): string {
  const s = summarise(ledger)
  const cards = (Object.keys(s.byState) as IssueState[])
    .filter((state) => s.byState[state] > 0)
    .map((state) => `<div class="card"><b>${s.byState[state]}</b><span>${state}</span></div>`)
    .join('')

  const rows = s.ranked
    .map(
      (i) => `<tr>
      <td>${escape(i.id)}</td>
      <td>${escape(i.title)}</td>
      <td>${escape(i.role)}</td>
      <td>${escape(i.route)}</td>
      <td>${escape(i.category)}</td>
      <td>${escape(i.severity)}</td>
      <td>${escape(i.risk)}</td>
      <td>${i.frequency}</td>
      <td>${escape(i.state)}</td>
    </tr>`,
    )
    .join('')

  return `<!doctype html>
<html lang="th"><head><meta charset="utf-8"><title>UX Lab</title>
<style>
  body { font-family: system-ui, sans-serif; margin: 2rem; color: #18181b; background: #fafafa }
  .cards { display: flex; gap: .75rem; flex-wrap: wrap; margin: 1rem 0 2rem }
  .card { background: #fff; border: 1px solid #e4e4e7; border-radius: .75rem; padding: .75rem 1rem; min-width: 7rem }
  .card b { display: block; font-size: 1.5rem }
  .card span { font-size: .75rem; color: #71717a }
  table { border-collapse: collapse; width: 100%; background: #fff }
  th, td { border: 1px solid #e4e4e7; padding: .4rem .6rem; text-align: left; font-size: .875rem }
  th { background: #f4f4f5 }
</style></head>
<body>
  <h1>UX Lab</h1>
  <p>ปรับปรุงล่าสุด ${escape(ledger.updatedAt)} · ค้างอยู่ ${s.open} เรื่อง</p>
  <div class="cards">${cards}</div>
  <table>
    <tr><th>รหัส</th><th>เรื่อง</th><th>บทบาท</th><th>หน้า</th><th>ประเภท</th><th>ความรุนแรง</th><th>ความเสี่ยง</th><th>เจอกี่ run</th><th>สถานะ</th></tr>
    ${rows}
  </table>
</body></html>
`
}
```

- [ ] **Step 4: Run it to verify it passes**

Run: `cd pizza-stock && npx vitest run tests/ux-lab-report.test.ts`
Expected: PASS, 6 tests.

- [ ] **Step 5: Teach the CLI to write the page**

In `scripts/ux-lab/cli.mjs`, add after the imports:

```js
import { writeFileSync } from 'node:fs'
import { renderReport } from './report.ts'
```

and, immediately after `const ledger = JSON.parse(...)`, add:

```js
if (process.argv[2] === 'report') {
  const out = join(LAB, 'status.html')
  writeFileSync(out, renderReport(ledger), 'utf8')
  console.log(`\n✓ เขียนหน้าสรุปแล้วที่ ${out}\n`)
  process.exit(0)
}
```

- [ ] **Step 6: Ignore the generated page**

Append to `.gitignore`:

```
# generated from docs/ux-lab/ledger.json by `npm run ux-lab report`
docs/ux-lab/status.html
```

- [ ] **Step 7: Generate it and look at it**

Run:
```bash
cd pizza-stock && npm run ux-lab report && npm run ux-lab
```
Expected: the write message, then the validation tick.

Open `docs/ux-lab/status.html` in the browser pane and confirm the counts match the ledger.

- [ ] **Step 8: Run the full gate**

Run:
```bash
cd pizza-stock && npx tsc -p tsconfig.app.json --noEmit && npx tsc -p scripts/tsconfig.json --noEmit && npm run lint && npm run i18n:check && npm test && npm run test:rules && npx tsc -p worker/tsconfig.json --noEmit && npm run build
```
Expected: all pass; `npm test` reports **707 passed** (701 + 6).

- [ ] **Step 9: Commit**

```bash
git add scripts/ux-lab/report.ts scripts/ux-lab/cli.mjs tests/ux-lab-report.test.ts .gitignore
git commit -m "UX lab: a status page generated from the ledger, not a second source of truth"
```

---

## Done when

1. `npm run ux-lab` exits 0 and every app route is covered by a scenario.
2. Both sweeps exist: 16 desktop run folders and 16 mobile ones, each with all seven metrics.
3. Every finding is a deduped issue in `ledger.json` with an honest `category`, `severity` and `risk`.
4. At least one issue reached `VERIFIED` through the full gate and a retest with measured before/after numbers.
5. `npm test` is green at 707 and nothing was merged to `main`.
